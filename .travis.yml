language: haskell
ghc:
  - 7.6
  - 7.8

before_install:
  - git clone --depth 1 https://github.com/Copilot-Language/copilot-core
  - cd copilot-core && cabal install --only-dependencies && cabal configure && cabal build && cabal install
